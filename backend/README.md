# ZONA VIP GYM — Backend

Servidor Express + PostgreSQL (Supabase). La guía completa de despliegue,
operación y arquitectura está en el [README principal](../README.md).

```bash
cp .env.example .env   # completar DATABASE_URL y JWT_SECRET
npm install
npm test               # pruebas sin base de datos
npm start
```
