{
  "name": "zona-vip-gym",
  "version": "2.0.0",
  "private": true,
  "description": "Sistema de gestión de ZONA VIP GYM (el código vive en backend/)",
  "scripts": {
    "postinstall": "cd backend && npm ci --omit=dev",
    "start": "node backend/server.js",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=18.17"
  }
}
